# DOCKET frontend env (all VITE_* values are PUBLIC — never put keys here)
VITE_RPC_URL=https://sepolia.base.org
# The deployed ReceiptRegistry — set after live deploy; ask flow stays disabled until then
VITE_REGISTRY_ADDRESS=
