# DOCKET frontend env (all VITE_* values are PUBLIC — never put keys here)
# Primary RPC (public Base Sepolia). The app also fails over to publicnode + drpc.
VITE_RPC_URL=https://sepolia.base.org
# The deployed ReceiptRegistry (source-verified on Blockscout). Ask flow + receipt
# reads activate only when set. Live deployment (2026-09-02, ask-to-answer binding):
VITE_REGISTRY_ADDRESS=0xb5Ed97b4F10da09B9b54594925F0Ba5b528BBf48
